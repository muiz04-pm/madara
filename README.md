# Приложение "Военная авторизация"

## Что это?
Десктопное приложение с графическим интерфейсом на Python (Tkinter), которое реализует систему авторизации и регистрации пользователей с проверкой сложности пароля. После успешного входа открывается окно с праздничным поздравлением в военно-патриотическом стиле, приуроченное к 23 февраля.

**Функционал:**
- Регистрация новых пользователей
- Вход в систему
- Проверка пароля на сложность (минимум 8 символов и наличие спецсимволов $, #, @)
- Отображение праздничного окна с изображением после авторизации

## Как клонировать проект
```bash
git clone https://github.com/Sement-000/auth-app.git
cd auth-app
```
Зависимости
Для работы приложения необходимо установить следующие библиотеки:
```bash
pip install Pillow
```
Примечание: Tkinter обычно входит в стандартную поставку Python, но если его нет:

- Windows:  обычно установлен по умолчанию
- Linux: ``` sudo apt-get install python3-tk ```
- macOS: ``` brew install python-tk ```

  Как запустить
  ```bash
  python tk.py
  ```
  Важно: Для корректного отображения праздничного окна поместите изображение   ``` 67.png ``` в ту же папку, где находится файл   ``` tk.py```.
  
| ФИО | Группа |
|------|--------|
| Амонбеков Муиз Амонбекович | 23ИСд |
